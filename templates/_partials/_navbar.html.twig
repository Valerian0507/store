{# Navbar #}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container">
		<div class="collapse navbar-collapse justify-content" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_product') }}">Produit</a>
				</li>
			</ul>
            <ul class="navbar-nav">
                {% if app.user %}
                <li class="nav-item">
                    <form class="d-inline" method="POST" action="{{ path('app_logout') }}">
                        <input type="hidden" name="csrf-token" value="{{ csrf_token('logout') }}">
                        <button type="submit" class="btn nav-link" title="Déconnexion" style="background: none; border: 0;">
                            <i class="fa-solid fa-right-from-bracket" style="color: #304880"></i>
                        </button>
                    </form>
                </li>
                {% else %}
                <li>
                    <a class="nav-link" href="{{ path('app_login') }}" title="Connexion">
                        <i class="fa-solid fa-arrow-right-to-bracket" style="color: #304880"></i>
                    </a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}" title="Inscription">
                        <i class="fa-solid fa-user-plus" style="color: #304880"></i>
                    </a>
                </li>
                {% endif %}
            </ul>
		</div>
	</div>
</nav>
