{# templates/_partials/_flash.html.twig #}
<div class="flash-container">
  {% for type, messages in app.flashes %}
    {% for message in messages %}
      <div class="alert alert-{{ type }} js-flash" role="alert">
        {{ message }}
      </div>
    {% endfor %}
  {% endfor %}
</div>
