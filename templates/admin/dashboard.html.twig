{% extends 'admin/adminBase.html.twig' %}

{% block title %}Admin · Dashboard{% endblock %}

{% block admin_content %}
    <section class="container">
        <h1>Admin dashboard</h1>

        <div class="list-group mt-3">
            <a class="list-group-item list-group-item-action" href="{{ path('admin_products_index') }}">
                Products
            </a>
            <br>
            <a class="list-group-item list-group-item-action" href="{{ path('admin_users_index') }}">
                Users
            </a>
        </div>
    </section>
{% endblock %}
